@model Category

@*Phải dùng section cho cả giao diện này luôn tại khi render trong layout là giao diện nào cũng phải dùng hết*@
@section title{
    @if(Model != null)
    {
        <h3>Category: @Model.Name</h3>
    }
}

@if (Model != null)
{
    <partial name="_Category" model="@Model" />
}